import React from 'react';
import  './Header.scss';

const Footer = props => {
    return (<div className="header">
              <span>Weather Application using React.js</span>
            </div>
            )
};

export default Footer;